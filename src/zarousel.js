import React from 'react';
import PropTypes from 'prop-types';

class Zarousel extends React.Component {
  static propTypes = {
  };

  static defaultProps = {

  };

  state = {

  };

  render() {
    return (
      <div>
        hello Zarousel
      </div>
    );
  }
}

export default Zarousel;
